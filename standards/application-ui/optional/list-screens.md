# 一覧画面

このOptional Standardは、並び替え、絞り込み、ページネーション、行選択などの対話的な一覧／検索画面を実装する場合に参照します。これらのうち一部だけを持つ画面も対象です。

業務システムで繰り返し現れる画面のため、実装手段と操作の判断を揃えます。ここでは判断基準だけを定義し、コンポーネントの実装手順は扱いません。

## 1. 実装手段の選択

| 画面の性質 | 実装 |
|---|---|
| 検索条件を送信して一覧を差し替えるだけで、クライアント側の複雑なUI状態を持たない | Django Template + htmx |
| ヘッダ操作、入力に追従する絞り込み、行選択など、クライアント側のUI状態を持つ | React Island。表UIは `shadcn/ui` のTable/Data Tableの考え方を基礎にし、必要に応じてTanStack Tableを利用する |

- htmxで十分な一覧を理由なくReact Islandへ移さない。
- UI状態を持つ表をhtmxと自前JavaScriptで複雑化しない。インタラクティブUIの標準手段はReact Islandsとする。
- `shadcn/ui` のData Tableは完成済みの単一コンポーネントではなく、TableとTanStack Tableを組み合わせる実装パターンとして扱う。

## 2. 絞り込み・並び替え・ページングの整合性

- サーバー側でページングする場合、絞り込み・並び替えもサーバー側で行う。現在ページのデータだけをクライアント側で並び替えて、全体が並び替わったように見せない。
- 全件をクライアントへ渡して処理してよいかは、ページングの有無だけでなく、データ量、権限、機密性、初期転送量を含めて判断する。
- サーバー側処理とクライアント側処理を混在させる場合は、ユーザーが見ている結果集合と操作対象が一致することを確認する。

大規模な一覧や性能問題を扱う場合、または明示的に性能確認を求められた場合は、N+1クエリ等について [Quality Recommendations](../../../recommendations/quality.md) を参照する。通常の小規模な一覧変更だけを理由に、Quality Recommendations全体を必須手順へ昇格させない。

## 3. 状態をURLへ反映する

並び替え、絞り込み条件、ページ番号は、再現性が必要な場合はクエリパラメータへ反映する。

- リロード、ブラウザバック、URL共有で同じ一覧を再現できる。
- サーバー側で処理する場合、URLパラメータをクエリの入力として利用でき、状態の二重管理を減らせる。
- パラメータはサーバー側で検証し、URL書き換えによって権限上見えない行や列が見えないようにする。

表示列などユーザー個人の表示設定は、URLへ載せるか永続化するかを画面ごとに判断してよい。

## 4. 揃える操作

- 絞り込みUIは表の上部など一貫した領域へまとめる。
- 並び替えは列ヘッダから行い、昇順・降順の状態を見て分かるようにする。
- ページネーションは表の下部を基本とする。
- 一覧全体の主操作はPage Header領域へ置き、行単位の操作は当該行内へ置く。
- 行選択を持つ場合、絞り込みやページ移動時の選択状態の扱いを明確にする。

## 5. データがない場合

Core StandardのEmpty Stateに従い、次を区別する。

- **元データが0件** — 有効な次の操作があれば示す。
- **絞り込み結果が0件** — 条件を緩める、または解除する手段を示す。

## 6. 共通化

- 同じ配線や操作が実際に繰り返された時点で、プロジェクト内の共通コンポーネント化を検討する。1画面目から先行抽象化しない。
- 共通化へ業務ドメインの知識を持ち込まない。
- 採用済みのApplication UI Kitに実装がある場合は再実装しない。

## 扱わないもの

- ライブラリの選定 — [Recommendations](../../../recommendations/frontend.md)
- Data Tableの具体的なセットアップ・API・バージョン移行 — ai-dev-playbook
- Excel的なセル編集を伴う一覧 — 必要性が確認されたプロジェクトで個別に設計する
